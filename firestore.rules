rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write for development testing in the sandboxed database instance
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
